package com.softserve.edu.opencart.tests.shopping_cart_tests;

import com.softserve.edu.opencart.tests.ATestRunner;
import org.testng.annotations.Test;

@Test
public class ShoppingCartTotalPriceTest extends ATestRunner {
    public void checkTotalPriceForProductWithQuantity() throws InterruptedException {
        // Steps
        //ShoppingCartProductsContainer shoppingCartProductsContainer = loadApplication()
        //        .addProductToCart(ProductRepository.getMacBook())
        //        .gotoShoppingCartPage()
        //        .getShoppingCartProductsContainer();
        //Thread.sleep(2000);
        //

        //System.out.println("aaa:" + (BigDecimal) shoppingCartProductsContainer.getUnitPriceByName(ProductRepository.getMacBook()));
        // System.out.println("exp " + expected);
        // Check
        //Assert.assertEquals(expected.add(expected), actual);
        //Thread.sleep(5000);
        // Steps
    }
}
